[project]
name = "ai-news-radar"
version = "0.1.0"
description = "Local financial news radar for early hard-signal detection."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

